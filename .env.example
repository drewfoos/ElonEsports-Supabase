# Supabase — get these from your Supabase project dashboard (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database (direct connection — for migrations/CLI only, not used at runtime)
DATABASE_URL=

# Admin — the single email address allowed to access /admin routes
ADMIN_EMAIL=

# start.gg — generate at https://start.gg/admin/profile/developer
STARTGG_API_TOKEN=
